import { n as createMiddleware } from "./ssr.mjs";
import { n as clsx } from "../_libs/class-variance-authority+clsx.mjs";
import { t as twMerge } from "../_libs/tailwind-merge.mjs";
//#region node_modules/.nitro/vite/services/ssr/assets/utils-B7wQydhJ.js
/**
* Auth middleware for server functions — the standard way to get the caller's
* verified user id. When deployed the session cookie is same-origin and rides
* along automatically. In the live preview the client also forwards the bearer
* token (partitioned cookies) via the `.client` hook below — call sites do not
* thread it themselves.
*
*   import { createServerFn } from "@tanstack/react-start";
*   import { getSql } from "@/lib/db";
*   import { authMiddleware } from "@/lib/auth/middleware";
*
*   export const listTodos = createServerFn({ method: "GET" })
*     .middleware([authMiddleware])
*     .handler(async ({ context }) => {
*       const sql = await getSql();
*       return sql`select * from todos where user_id = ${context.userId}`;
*     });
*
* Signed out with auth on (live preview included) -> throws `UnauthorizedError`
* (see `verify.server.ts`). With auth disabled (`VITE_AUTH_ENABLED=false`, the
* shipped default) it resolves the shared dev user — but throws instead when a
* `DATABASE_URL` is also set, so an app without sign-in must not use this at
* all. On the auth-on path, use it on every server function that touches
* per-user data and scope every query by `context.userId`.
*/
var authMiddleware = createMiddleware({ type: "function" }).client(async ({ next }) => {
	const { getBearerToken } = await import("./client-B40BzJxt.mjs").then((n) => n.n);
	return next({ sendContext: { bearerToken: getBearerToken() ?? void 0 } });
}).server(async ({ next, context }) => {
	const { assertSameSiteRequest } = await import("./isolation.server-CGNg1r0B.mjs");
	const { requireUserId } = await import("./verify.server-Rzum7ZJe.mjs");
	assertSameSiteRequest();
	return next({ context: { userId: await requireUserId(context.bearerToken) } });
});
var _0001_auth_default = "-- Better Auth schema (identity + sessions for \"Sign in with Grok\").\n--\n-- Generated by the Better Auth CLI for its Postgres adapter — DO NOT EDIT by\n-- hand. `@/lib/auth/server` runs Better Auth against these tables when\n-- DATABASE_URL is set. The columns are camelCase and MUST stay double-quoted so\n-- Postgres preserves the case Better Auth queries by.\n--\n-- Migrations in this folder are the single source of truth for your schema. They\n-- apply to Neon during the Vercel build (`npm run build`) and to the local\n-- PGLite fallback automatically on startup, so dev matches production. Applied\n-- files are recorded by name in `_migrations` and NEVER run again.\n--\n-- Put YOUR app's schema in NEW ordered files (0002_*.sql, 0003_*.sql, …), never\n-- in this one. For app tables, prefer snake_case and give per-user tables a\n-- `user_id TEXT NOT NULL` column (TEXT, not UUID — the preview dev user id is\n-- the string 'dev-user'), then scope every query to the authenticated user\n-- server-side (see the `neon` + `auth` skills and src/lib/auth/verify.server.ts).\n\ncreate table if not exists \"user\" (\n  \"id\" text not null primary key,\n  \"name\" text not null,\n  \"email\" text not null unique,\n  \"emailVerified\" boolean not null,\n  \"image\" text,\n  \"createdAt\" timestamptz default CURRENT_TIMESTAMP not null,\n  \"updatedAt\" timestamptz default CURRENT_TIMESTAMP not null\n);\n\ncreate table if not exists \"session\" (\n  \"id\" text not null primary key,\n  \"expiresAt\" timestamptz not null,\n  \"token\" text not null unique,\n  \"createdAt\" timestamptz default CURRENT_TIMESTAMP not null,\n  \"updatedAt\" timestamptz not null,\n  \"ipAddress\" text,\n  \"userAgent\" text,\n  \"userId\" text not null references \"user\" (\"id\") on delete cascade\n);\n\ncreate table if not exists \"account\" (\n  \"id\" text not null primary key,\n  \"accountId\" text not null,\n  \"providerId\" text not null,\n  \"userId\" text not null references \"user\" (\"id\") on delete cascade,\n  \"accessToken\" text,\n  \"refreshToken\" text,\n  \"idToken\" text,\n  \"accessTokenExpiresAt\" timestamptz,\n  \"refreshTokenExpiresAt\" timestamptz,\n  \"scope\" text,\n  \"password\" text,\n  \"createdAt\" timestamptz default CURRENT_TIMESTAMP not null,\n  \"updatedAt\" timestamptz not null\n);\n\ncreate table if not exists \"verification\" (\n  \"id\" text not null primary key,\n  \"identifier\" text not null,\n  \"value\" text not null,\n  \"expiresAt\" timestamptz not null,\n  \"createdAt\" timestamptz default CURRENT_TIMESTAMP not null,\n  \"updatedAt\" timestamptz default CURRENT_TIMESTAMP not null\n);\n\ncreate index if not exists \"session_userId_idx\" on \"session\" (\"userId\");\ncreate index if not exists \"account_userId_idx\" on \"account\" (\"userId\");\ncreate index if not exists \"verification_identifier_idx\" on \"verification\" (\"identifier\");\n";
var _0002_schema_default = "-- Pulse core schema\ncreate table if not exists profiles (\n  user_id text primary key,\n  handle text not null unique,\n  display_name text not null,\n  bio text not null default '',\n  avatar_url text,\n  is_seed boolean not null default false,\n  posting_locked boolean not null default false,\n  banned boolean not null default false,\n  ban_reason text,\n  ban_category text,\n  banned_at timestamptz,\n  strike_count integer not null default 0,\n  created_at timestamptz not null default now()\n);\n\ncreate table if not exists videos (\n  id text primary key,\n  user_id text not null,\n  title text not null,\n  description text not null default '',\n  tags text not null default '',\n  src_url text not null,\n  poster_url text,\n  duration_sec integer not null default 0,\n  is_short boolean not null default false,\n  status text not null default 'published',\n  takedown_reason text,\n  takedown_category text,\n  view_count integer not null default 0,\n  like_count integer not null default 0,\n  comment_count integer not null default 0,\n  created_at timestamptz not null default now()\n);\n\ncreate index if not exists videos_user_id_idx on videos (user_id);\ncreate index if not exists videos_status_created_idx on videos (status, created_at desc);\ncreate index if not exists videos_short_idx on videos (is_short, status);\n\ncreate table if not exists comments (\n  id text primary key,\n  video_id text not null,\n  user_id text not null,\n  body text not null,\n  status text not null default 'visible',\n  created_at timestamptz not null default now()\n);\n\ncreate index if not exists comments_video_idx on comments (video_id, created_at);\n\ncreate table if not exists likes (\n  user_id text not null,\n  video_id text not null,\n  created_at timestamptz not null default now(),\n  primary key (user_id, video_id)\n);\n\ncreate table if not exists follows (\n  follower_id text not null,\n  creator_id text not null,\n  created_at timestamptz not null default now(),\n  primary key (follower_id, creator_id)\n);\n\ncreate table if not exists reports (\n  id text primary key,\n  reporter_id text not null,\n  video_id text,\n  comment_id text,\n  reason text not null,\n  details text not null default '',\n  status text not null default 'open',\n  created_at timestamptz not null default now()\n);\n\ncreate table if not exists moderation_events (\n  id text primary key,\n  user_id text,\n  video_id text,\n  comment_id text,\n  action text not null,\n  category text not null,\n  severity text not null,\n  details text not null default '',\n  created_at timestamptz not null default now()\n);\n\ncreate index if not exists moderation_events_created_idx on moderation_events (created_at desc);\n";
var _0003_seed_default = "-- Seed creators, catalog, comments, and a Guard enforcement sample.\ninsert into profiles (user_id, handle, display_name, bio, avatar_url, is_seed)\nvalues\n  ('seed-northline', 'northline', 'Northline', 'Ridges, weather, and the long quiet of the north.', '/avatars/northline.jpg', true),\n  ('seed-fieldnotes', 'fieldnotes', 'Field Notes', 'Food, craft, and the small rituals of a kitchen.', '/avatars/fieldnotes.jpg', true),\n  ('seed-nightshift', 'nightshift', 'Night Shift', 'Cities after the lights take over.', '/avatars/nightshift.jpg', true),\n  ('seed-openroad', 'openroad', 'Open Road', 'Empty miles. Heat. Horizon.', '/avatars/openroad.jpg', true),\n  ('seed-atlas', 'atlas', 'Studio Atlas', 'Independent animation and short film.', '/avatars/atlas.jpg', true),\n  ('seed-harbor', 'harbor', 'Harbor Light', 'Water, weather, and the edge of the map.', '/avatars/harbor.jpg', true),\n  ('seed-quiet', 'quiethours', 'Quiet Hours', 'Rooms, linen, and late morning light.', '/avatars/quiethours.jpg', true),\n  ('seed-pulse', 'pulse', 'Pulse', 'The house channel. Safety, product, and originals.', '/avatars/pulse.jpg', true)\non conflict (user_id) do nothing;\n\ninsert into videos (id, user_id, title, description, tags, src_url, poster_url, duration_sec, is_short, status, view_count, like_count, comment_count, created_at)\nvalues\n  ('v-sintel', 'seed-atlas', 'Sintel',\n   'A short film from the Blender Foundation. A young woman, a dragon, and a world cut from stone and sky. CC-BY.',\n   'film,animation,fantasy',\n   'https://commondatastorage.googleapis.com/gtv-videos-bucket/sample/Sintel.mp4',\n   'https://commondatastorage.googleapis.com/gtv-videos-bucket/sample/images/Sintel.jpg',\n   888, false, 'published', 4823310, 192004, 2, now() - interval '18 days'),\n  ('v-bunny', 'seed-atlas', 'Big Buck Bunny',\n   'The Peach Open Movie. A very large rabbit, a very small grievance. CC-BY.',\n   'film,animation,comedy',\n   'https://commondatastorage.googleapis.com/gtv-videos-bucket/sample/BigBuckBunny.mp4',\n   'https://commondatastorage.googleapis.com/gtv-videos-bucket/sample/images/BigBuckBunny.jpg',\n   596, false, 'published', 9102201, 301442, 2, now() - interval '32 days'),\n  ('v-tears', 'seed-atlas', 'Tears of Steel',\n   'Live action and visual effects from Amsterdam. Robots, rain, and a city that will not stay in the past. CC-BY.',\n   'film,scifi,vfx',\n   'https://commondatastorage.googleapis.com/gtv-videos-bucket/sample/TearsOfSteel.mp4',\n   'https://commondatastorage.googleapis.com/gtv-videos-bucket/sample/images/TearsOfSteel.jpg',\n   734, false, 'published', 2210884, 88012, 1, now() - interval '11 days'),\n  ('v-elephants', 'seed-atlas', 'Elephants Dream',\n   'The first open movie. Two people inside a machine that may be a mind. CC-BY.',\n   'film,animation,experimental',\n   'https://commondatastorage.googleapis.com/gtv-videos-bucket/sample/ElephantsDream.mp4',\n   'https://commondatastorage.googleapis.com/gtv-videos-bucket/sample/images/ElephantsDream.jpg',\n   654, false, 'published', 1204412, 44190, 1, now() - interval '40 days'),\n  ('v-joyrides', 'seed-openroad', 'Joyride, late light',\n   'A stretch of tarmac and the last hour of sun. No destination worth naming.',\n   'travel,driving,dusk',\n   'https://commondatastorage.googleapis.com/gtv-videos-bucket/sample/ForBiggerJoyrides.mp4',\n   'https://commondatastorage.googleapis.com/gtv-videos-bucket/sample/images/ForBiggerJoyrides.jpg',\n   15, false, 'published', 802114, 29110, 1, now() - interval '3 days'),\n  ('v-escapes', 'seed-openroad', 'Escapes',\n   'Leave the grid. The landscape does the rest.',\n   'travel,landscape',\n   'https://commondatastorage.googleapis.com/gtv-videos-bucket/sample/ForBiggerEscapes.mp4',\n   'https://commondatastorage.googleapis.com/gtv-videos-bucket/sample/images/ForBiggerEscapes.jpg',\n   15, false, 'published', 655201, 18840, 0, now() - interval '6 days'),\n  ('v-subaru', 'seed-openroad', 'Street and dirt',\n   'A car that does not mind which surface you give it. Dust, gravel, then the road again.',\n   'travel,motorsport',\n   'https://commondatastorage.googleapis.com/gtv-videos-bucket/sample/SubaruOutbackOnStreetAndDirt.mp4',\n   'https://commondatastorage.googleapis.com/gtv-videos-bucket/sample/images/SubaruOutbackOnStreetAndDirt.jpg',\n   30, false, 'published', 440128, 12004, 0, now() - interval '9 days'),\n  ('v-meltdowns', 'seed-nightshift', 'Meltdown hour',\n   'When the city overheats. Glass, signal, and a sky that will not cool.',\n   'city,night,editorial',\n   'https://commondatastorage.googleapis.com/gtv-videos-bucket/sample/ForBiggerMeltdowns.mp4',\n   'https://commondatastorage.googleapis.com/gtv-videos-bucket/sample/images/ForBiggerMeltdowns.jpg',\n   15, false, 'published', 990441, 33012, 1, now() - interval '2 days'),\n  ('v-fun', 'seed-quiet', 'For a larger quiet',\n   'A short on rooms, color, and staying in.',\n   'lifestyle,interior',\n   'https://commondatastorage.googleapis.com/gtv-videos-bucket/sample/ForBiggerFun.mp4',\n   'https://commondatastorage.googleapis.com/gtv-videos-bucket/sample/images/ForBiggerFun.jpg',\n   15, false, 'published', 210334, 8801, 0, now() - interval '5 days'),\n  ('s-cliff', 'seed-harbor', 'Cliff swell',\n   'The Pacific does not ask. Six seconds of impact against volcanic rock.',\n   'ocean,nature,drop',\n   '/shorts/cliff-swell.mp4',\n   '/posters/cliff-swell.jpg',\n   6, true, 'published', 2408810, 198440, 2, now() - interval '1 day'),\n  ('s-blazes', 'seed-northline', 'High desert firelight',\n   'A burn that stays in the bowl of the land. Watch the sky go copper.',\n   'desert,night,drop',\n   'https://commondatastorage.googleapis.com/gtv-videos-bucket/sample/ForBiggerBlazes.mp4',\n   'https://commondatastorage.googleapis.com/gtv-videos-bucket/sample/images/ForBiggerBlazes.jpg',\n   15, true, 'published', 1102044, 77012, 1, now() - interval '20 hours'),\n  ('s-joy', 'seed-openroad', 'Center line',\n   'Keep the yellow line in the lower third and do not look away.',\n   'driving,drop,dusk',\n   'https://commondatastorage.googleapis.com/gtv-videos-bucket/sample/ForBiggerJoyrides.mp4',\n   'https://commondatastorage.googleapis.com/gtv-videos-bucket/sample/images/ForBiggerJoyrides.jpg',\n   15, true, 'published', 880120, 54010, 0, now() - interval '8 hours'),\n  ('s-escape', 'seed-northline', 'Leave now',\n   'A fifteen-second reminder that the map still has empty cells.',\n   'travel,drop',\n   'https://commondatastorage.googleapis.com/gtv-videos-bucket/sample/ForBiggerEscapes.mp4',\n   'https://commondatastorage.googleapis.com/gtv-videos-bucket/sample/images/ForBiggerEscapes.jpg',\n   15, true, 'published', 701204, 41008, 0, now() - interval '4 hours'),\n  ('s-melt', 'seed-nightshift', 'Signal storm',\n   'Vertical crop of a city that will not sleep. Drop, mute, keep going.',\n   'city,drop',\n   'https://commondatastorage.googleapis.com/gtv-videos-bucket/sample/ForBiggerMeltdowns.mp4',\n   'https://commondatastorage.googleapis.com/gtv-videos-bucket/sample/images/ForBiggerMeltdowns.jpg',\n   15, true, 'published', 1560021, 99004, 1, now() - interval '14 hours'),\n  ('s-fun', 'seed-quiet', 'Stay in',\n   'A small room, a large quiet. Loop it.',\n   'lifestyle,drop',\n   'https://commondatastorage.googleapis.com/gtv-videos-bucket/sample/ForBiggerFun.mp4',\n   'https://commondatastorage.googleapis.com/gtv-videos-bucket/sample/images/ForBiggerFun.jpg',\n   15, true, 'published', 330120, 18004, 0, now() - interval '2 hours'),\n  ('v-removed', 'seed-pulse', 'Removed by Pulse Guard',\n   'This title was a policy sample. It never ran in the public feed.',\n   'safety',\n   'https://commondatastorage.googleapis.com/gtv-videos-bucket/sample/ForBiggerFun.mp4',\n   'https://commondatastorage.googleapis.com/gtv-videos-bucket/sample/images/ForBiggerFun.jpg',\n   15, false, 'taken_down', 0, 0, 0, now() - interval '12 hours')\non conflict (id) do nothing;\n\ninsert into comments (id, video_id, user_id, body, status, created_at)\nvalues\n  ('c1', 'v-sintel', 'seed-northline', 'The scale of those landscapes still surprises me.', 'visible', now() - interval '4 days'),\n  ('c2', 'v-sintel', 'seed-harbor', 'Watch it once for the story, once for the light.', 'visible', now() - interval '3 days'),\n  ('c3', 'v-bunny', 'seed-quiet', 'This is the one I put on when the room needs color.', 'visible', now() - interval '10 days'),\n  ('c4', 'v-bunny', 'seed-openroad', 'The butterfly still gets me.', 'visible', now() - interval '8 days'),\n  ('c5', 'v-tears', 'seed-nightshift', 'Amsterdam as a character. The rain is doing overtime.', 'visible', now() - interval '2 days'),\n  ('c6', 'v-elephants', 'seed-pulse', 'A machine with a memory. Still the strangest open movie.', 'visible', now() - interval '12 days'),\n  ('c7', 'v-joyrides', 'seed-harbor', 'That last hour of sun on the hood.', 'visible', now() - interval '1 day'),\n  ('c8', 'v-meltdowns', 'seed-atlas', 'The edit is mean in the best way.', 'visible', now() - interval '20 hours'),\n  ('c9', 's-cliff', 'seed-northline', 'You can feel the cold off the spray.', 'visible', now() - interval '10 hours'),\n  ('c10', 's-cliff', 'seed-quiet', 'Looping this on mute at 1am. No notes.', 'visible', now() - interval '6 hours'),\n  ('c11', 's-blazes', 'seed-openroad', 'Copper sky. That is the whole argument.', 'visible', now() - interval '12 hours'),\n  ('c12', 's-melt', 'seed-fieldnotes', 'Drop is dangerous. I was here for forty minutes.', 'visible', now() - interval '5 hours')\non conflict (id) do nothing;\n\ninsert into follows (follower_id, creator_id)\nvalues\n  ('seed-harbor', 'seed-northline'),\n  ('seed-quiet', 'seed-fieldnotes'),\n  ('seed-nightshift', 'seed-atlas'),\n  ('seed-openroad', 'seed-harbor'),\n  ('seed-fieldnotes', 'seed-quiet'),\n  ('seed-atlas', 'seed-northline')\non conflict do nothing;\n\ninsert into moderation_events (id, user_id, video_id, action, category, severity, details, created_at)\nvalues\n  ('m1', 'seed-pulse', 'v-removed', 'takedown', 'graphic-violence', 'severe',\n   'Pulse Guard removed this sample listing to demonstrate a public takedown. No account was harmed.',\n   now() - interval '12 hours')\non conflict (id) do nothing;\n";
/**
* Migration bookkeeping shared by the two appliers — `scripts/migrate.mjs`
* (deploy, `readdir`) and `src/lib/db.ts` (PGLite preview, `import.meta.glob`).
*
* Applied files are keyed by BASENAME, so the same file applies once no matter
* which directory it is globbed from. That is what makes the auth schema safe to
* copy from `migrations/auth/` into `migrations/` when an app turns sign-in on:
* a database that already has `0001_auth.sql` will not re-run it.
*
* Neither applier descends into subdirectories, so `migrations/auth/*.sql` is
* out of scope for both until it is copied up.
*/
/**
* The `_migrations` key for a migration path (or bare filename).
* @param {string} path
* @returns {string}
*/
function migrationName(path) {
	return path.split("/").pop() ?? path;
}
/**
* @param {string} path
* @returns {boolean}
*/
function isMigrationFile(path) {
	return path.endsWith(".sql");
}
/**
* Migrations in `paths` that are not yet in `applied`, in apply order.
* Non-`.sql` entries (a `readdir` also yields `migrations/auth/`) are dropped.
* @param {Iterable<string>} paths
* @param {Iterable<string>} applied
* @returns {Array<{ name: string, path: string }>}
*/
function pendingMigrations(paths, applied) {
	const done = new Set(applied);
	return [...paths].filter(isMigrationFile).map((path) => ({
		name: migrationName(path),
		path
	})).sort((a, b) => a.name.localeCompare(b.name)).filter(({ name }) => !done.has(name));
}
var rawDatabaseUrl = typeof process !== "undefined" ? process.env.DATABASE_URL : void 0;
var databaseUrl = rawDatabaseUrl && rawDatabaseUrl.trim() ? rawDatabaseUrl : void 0;
/**
* Active backend: real **Neon** when `DATABASE_URL` is set (deployed / configured
* sandbox), otherwise a local embedded **PGLite** (Postgres compiled to WASM) so
* the app has a working database even with nothing configured — the live preview
* included. Swap in Neon later by just setting `DATABASE_URL`; no code changes.
*/
var dbSource = databaseUrl ? "neon" : "pglite";
/**
* Init state lives on globalThis as promises: dev HMR creates new instances of
* this module, and two instances racing module-level state would open a second
* pool or run two concurrent PGLite migration passes (whose duplicate
* `_migrations` insert rejects — and would get memoized, poisoning every later
* `getSql()`). A failed init clears its slot so the next call retries.
*/
var globalRef = globalThis;
/**
* Result-type parity: Postgres sends every value as text plus a type OID — the
* JS value is the DRIVER's parsing choice, and pg and PGLite disagree (pg:
* int8 -> string, date -> local-midnight Date; PGLite: int8 -> BigInt, which
* JSON.stringify rejects, date -> UTC Date). Normalize both so preview and
* production return identical, JSON-safe shapes:
*   int8/bigint (incl. count(*)) -> number (past 2^53 loses precision — cast
*                                   `::text` if you ever need huge integers)
*   date                         -> 'YYYY-MM-DD' string
*   interval                     -> Postgres interval text
* numeric already comes back as a string on both (arbitrary precision).
*/
var OID_INT8 = 20;
var OID_DATE = 1082;
var OID_INTERVAL = 1186;
var identity = (v) => v;
/** Wrap a query runner in the tagged-template + `.query()` `Sql` surface. */
function toSql(run) {
	const sql = (async (strings, ...values) => {
		let text = strings[0];
		for (let i = 0; i < values.length; i += 1) text += `$${i + 1}${strings[i + 1]}`;
		return run(text, values);
	});
	sql.query = (text, params = []) => run(text, params);
	return sql;
}
function createNeonSql() {
	globalRef.__pgSqlPromise__ ??= (async () => {
		const { Pool, types } = await import("../_libs/pg.mjs").then((n) => n.n);
		types.setTypeParser(OID_INT8, Number);
		types.setTypeParser(OID_DATE, identity);
		types.setTypeParser(OID_INTERVAL, identity);
		const pool = new Pool({ connectionString: databaseUrl });
		return toSql(async (text, params) => {
			return (await pool.query(text, params)).rows;
		});
	})().catch((err) => {
		globalRef.__pgSqlPromise__ = void 0;
		throw err;
	});
	return globalRef.__pgSqlPromise__;
}
async function createPgliteSql() {
	globalRef.__pgliteInstance__ ??= (async () => {
		const { PGlite } = await import("../_libs/electric-sql__pglite.mjs").then((n) => n.t);
		const pg = new PGlite({ parsers: {
			[OID_INT8]: Number,
			[OID_DATE]: identity,
			[OID_INTERVAL]: identity
		} });
		await pg.waitReady;
		await pg.exec("create table if not exists _migrations (name text primary key, applied_at timestamptz not null default now())");
		return pg;
	})().catch((err) => {
		globalRef.__pgliteInstance__ = void 0;
		throw err;
	});
	const pg = await globalRef.__pgliteInstance__;
	const migrate = async () => {
		const migrations = /* #__PURE__ */ Object.assign({
			"/migrations/0001_auth.sql": _0001_auth_default,
			"/migrations/0002_schema.sql": _0002_schema_default,
			"/migrations/0003_seed.sql": _0003_seed_default
		});
		const done = (await pg.query("select name from _migrations")).rows.map((r) => r.name);
		for (const { name, path } of pendingMigrations(Object.keys(migrations), done)) await pg.transaction(async (tx) => {
			await tx.exec(migrations[path]);
			await tx.query("insert into _migrations (name) values ($1)", [name]);
		});
	};
	const pass = (globalRef.__pgliteMigrateChain__ ?? Promise.resolve()).catch(() => void 0).then(migrate);
	globalRef.__pgliteMigrateChain__ = pass;
	await pass;
	return toSql(async (text, params) => {
		return (await pg.query(text, params)).rows;
	});
}
var sqlPromise = null;
async function createSql() {
	if (typeof window !== "undefined") throw new Error("@/lib/db is server-only — call getSql() from a createServerFn handler or a server route loader, never from client code.");
	return dbSource === "neon" ? createNeonSql() : createPgliteSql();
}
/**
* Get the shared, **server-only** SQL client. Neon when `DATABASE_URL` is set,
* otherwise the local PGLite fallback. Memoized — safe to call per request.
*
* Schema comes from `migrations/*.sql`, auto-applied before the first query on
* both backends — define tables there, never inline in server functions.
*/
function getSql() {
	sqlPromise ??= createSql().catch((err) => {
		sqlPromise = null;
		throw err;
	});
	return sqlPromise;
}
/**
* The shared PGLite instance (preview only), with `migrations/*.sql` applied.
* Lets Better Auth persist to the SAME embedded DB as app data in preview (via a
* Kysely dialect). Throws when `DATABASE_URL` is set (that path uses Neon).
*/
async function getPglite() {
	if (dbSource !== "pglite") throw new Error("getPglite() is only available on the PGLite fallback (no DATABASE_URL)");
	await getSql();
	const pg = await globalRef.__pgliteInstance__;
	if (!pg) throw new Error("PGLite instance failed to initialize");
	return pg;
}
/**
* Finish DB bootstrap before the server handles traffic.
*
* - **PGLite** (preview / no `DATABASE_URL`): open the in-memory DB and apply
*   `migrations/*.sql`. Idempotent — concurrent callers share one promise.
* - **Neon**: no-op (pool is created lazily on first query).
*
* Vite `configureServer` awaits this at dev startup; production imports of this
* module kick it off immediately (see bottom of file).
*/
function ensureDbReady() {
	if (dbSource !== "pglite") return Promise.resolve();
	return getSql().then(() => void 0);
}
var globalBoot = globalThis;
if (typeof window === "undefined" && dbSource === "pglite") globalBoot.__pgBootstrapPromise__ ??= ensureDbReady().catch((err) => {
	globalBoot.__pgBootstrapPromise__ = void 0;
	console.error("[db] PGLite bootstrap failed:", err);
	throw err;
});
function cn(...inputs) {
	return twMerge(clsx(inputs));
}
function compactNumber(n) {
	if (n < 1e3) return String(n);
	if (n < 1e4) return `${(n / 1e3).toFixed(1).replace(/\.0$/, "")}K`;
	if (n < 1e6) return `${Math.round(n / 1e3)}K`;
	if (n < 1e7) return `${(n / 1e6).toFixed(1).replace(/\.0$/, "")}M`;
	return `${Math.round(n / 1e6)}M`;
}
function formatDuration(totalSec) {
	if (!totalSec || totalSec < 0) return "0:00";
	const s = Math.round(totalSec);
	const h = Math.floor(s / 3600);
	const m = Math.floor(s % 3600 / 60);
	const sec = s % 60;
	if (h > 0) return `${h}:${String(m).padStart(2, "0")}:${String(sec).padStart(2, "0")}`;
	return `${m}:${String(sec).padStart(2, "0")}`;
}
function timeAgo(iso) {
	const then = new Date(iso).getTime();
	if (Number.isNaN(then)) return "";
	const diff = Math.max(0, Date.now() - then);
	const min = Math.floor(diff / 6e4);
	if (min < 1) return "now";
	if (min < 60) return `${min}m ago`;
	const hr = Math.floor(min / 60);
	if (hr < 24) return `${hr}h ago`;
	const day = Math.floor(hr / 24);
	if (day < 7) return `${day}d ago`;
	const wk = Math.floor(day / 7);
	if (wk < 5) return `${wk}w ago`;
	const mo = Math.floor(day / 30);
	if (mo < 12) return `${mo}mo ago`;
	return `${Math.floor(day / 365)}y ago`;
}
function slugify(input) {
	return input.toLowerCase().replace(/[^a-z0-9]+/g, "").slice(0, 18) || "user";
}
//#endregion
export { formatDuration as a, slugify as c, ensureDbReady as i, timeAgo as l, cn as n, getPglite as o, compactNumber as r, getSql as s, authMiddleware as t };
